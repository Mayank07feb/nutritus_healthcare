<!-- Sidebar -->
<aside id="sidebar"
    class="py-16 fixed left-0 top-0 h-full w-80 bg-white shadow-2xl z-50 transform -translate-x-full lg:translate-x-0 transition-transform duration-300 overflow-y-auto">

    <!-- Close button for mobile -->
    <button id="closeSidebar"
        class="absolute top-20 right-4 lg:hidden text-white bg-primary p-3 rounded-full shadow-lg z-50"
        aria-label="Close sidebar menu">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
    </button>


    <!-- Sidebar Header -->
    <div class="p-6 border-b border-gray-200">
        <h2 class="text-2xl font-montserrat font-bold text-secondary">HTML Tutorial</h2>
        <p class="text-gray-600 text-sm mt-1">Complete learning guide</p>
    </div>

    <!-- Progress Bar -->
    <div class="p-6 border-b border-gray-200">
        <div class="flex items-center justify-between mb-2">
            <span class="text-sm font-semibold text-gray-700">Progress</span>
            <span class="text-sm font-semibold text-primary">25%</span>
        </div>
        <div class="w-full bg-gray-200 rounded-full h-2">
            <div class="bg-primary h-2 rounded-full" style="width: 25%"></div>
        </div>
        <p class="text-xs text-gray-500 mt-1">3 of 12 lessons completed</p>
    </div>

    <!-- Navigation Menu -->
    <nav class="p-4">
        <div class="space-y-2">
            <div class="mb-4">
                <h3 class="text-lg font-semibold text-gray-800 mb-2 px-2">Getting Started</h3>
                <a href="#intro"
                    class="nav-item active flex items-center px-4 py-3 text-sm rounded-lg bg-primary text-white">
                    <span class="w-2 h-2 bg-white rounded-full mr-3"></span>
                    Introduction to HTML
                </a>
                <a href="#setup"
                    class="nav-item flex items-center px-4 py-3 text-sm text-gray-700 hover:bg-gray-100 rounded-lg">
                    <span class="w-2 h-2 bg-gray-400 rounded-full mr-3"></span>
                    Setting up Environment
                </a>
                <a href="#structure"
                    class="nav-item flex items-center px-4 py-3 text-sm text-gray-700 hover:bg-gray-100 rounded-lg">
                    <span class="w-2 h-2 bg-gray-400 rounded-full mr-3"></span>
                    HTML Document Structure
                </a>
            </div>
            <div class="mb-4">
                <h3 class="text-lg font-semibold text-gray-800 mb-2 px-2">Basic Elements</h3>
                <a href="#headings"
                    class="nav-item flex items-center px-4 py-3 text-sm text-gray-700 hover:bg-gray-100 rounded-lg">
                    <span class="w-2 h-2 bg-gray-400 rounded-full mr-3"></span>
                    Headings & Text
                </a>
                <a href="#paragraphs"
                    class="nav-item flex items-center px-4 py-3 text-sm text-gray-700 hover:bg-gray-100 rounded-lg">
                    <span class="w-2 h-2 bg-gray-400 rounded-full mr-3"></span>
                    Paragraphs & Formatting
                </a>
                <a href="#links"
                    class="nav-item flex items-center px-4 py-3 text-sm text-gray-700 hover:bg-gray-100 rounded-lg">
                    <span class="w-2 h-2 bg-gray-400 rounded-full mr-3"></span>
                    Links & Navigation
                </a>
                <a href="#images"
                    class="nav-item flex items-center px-4 py-3 text-sm text-gray-700 hover:bg-gray-100 rounded-lg">
                    <span class="w-2 h-2 bg-gray-400 rounded-full mr-3"></span>
                    Images & Media
                </a>
            </div>
        </div>
    </nav>

    <!-- Quick Actions -->
    <div class="p-4 border-t border-gray-200 mt-auto">
        <div class="space-y-2">
            <button
                class="w-full bg-primary text-white py-2 px-4 rounded-lg hover:bg-primary-hover transition-colors text-sm font-semibold">
                Take Quiz
            </button>
            <button
                class="w-full border border-gray-300 text-gray-700 py-2 px-4 rounded-lg hover:bg-gray-50 transition-colors text-sm font-semibold">
                Download PDF
            </button>
        </div>
    </div>
</aside>

<!-- Overlay -->
<div id="overlay" class="fixed inset-0 bg-black bg-opacity-50 z-40 hidden lg:hidden"></div>

<!-- Bottom Bar for Mobile -->
<div class="fixed bottom-0 left-0 w-full bg-primary z-50 lg:hidden">
    <div class="flex justify-center py-3">
        <button id="bottomToggleBtn"
            class="text-white font-bold px-6 py-2 rounded-full shadow hover:bg-primary-hover transition"
            aria-label="Open sidebar menu">
            ☰ Menu
        </button>
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const sidebar = document.getElementById('sidebar');
        const closeSidebar = document.getElementById('closeSidebar');
        const overlay = document.getElementById('overlay');
        const bottomToggleBtn = document.getElementById('bottomToggleBtn');

        function openSidebar() {
            sidebar.classList.remove('-translate-x-full');
            overlay.classList.remove('hidden');
            document.body.classList.add('overflow-hidden');
        }

        function closeSidebarFn() {
            sidebar.classList.add('-translate-x-full');
            overlay.classList.add('hidden');
            document.body.classList.remove('overflow-hidden');
        }

        bottomToggleBtn.addEventListener('click', openSidebar);
        closeSidebar.addEventListener('click', closeSidebarFn);
        overlay.addEventListener('click', closeSidebarFn);

        // Navigation active states
        const navItems = document.querySelectorAll('.nav-item');
        navItems.forEach(item => {
            item.addEventListener('click', function (e) {
                e.preventDefault();
                navItems.forEach(nav => {
                    nav.classList.remove('active', 'bg-primary', 'text-white');
                    nav.classList.add('text-gray-700', 'hover:bg-gray-100');
                    nav.querySelector('span').classList.remove('bg-white');
                    nav.querySelector('span').classList.add('bg-gray-400');
                });
                this.classList.add('active', 'bg-primary', 'text-white');
                this.classList.remove('text-gray-700', 'hover:bg-gray-100');
                this.querySelector('span').classList.add('bg-white');
                this.querySelector('span').classList.remove('bg-gray-400');

                // Close sidebar on mobile after click
                if (window.innerWidth < 1024) {
                    closeSidebarFn();
                }
            });
        });

        // Smooth scroll
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        });
    });
</script>